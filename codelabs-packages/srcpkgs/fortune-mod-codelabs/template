# Template file for 'fortune-mod-codelabs'
pkgname=fortune-mod-codelabs
version=20200307
revision=2
create_wrksrc=yes
hostmakedepends="fortune-mod"
depends="${hostmakedepends}"
short_desc="Quotes from #xbps and other CODE LABS related stuff"
maintainer="Georg Schabel <gescha@posteo.de>"
license="GPL-2.0-or-later"
homepage="http://codelabs.org/"

do_build() {
	cp ${FILESDIR}/void ${wrksrc}
	strfile void
}

do_install() {
	vmkdir usr/share/fortunes
	vcopy * usr/share/fortunes
}
