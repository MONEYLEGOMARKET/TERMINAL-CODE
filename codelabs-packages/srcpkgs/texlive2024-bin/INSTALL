case "${ACTION}" in
post)
	case "${UPDATE}" in
	no)
		cd opt/texlive2024-installer
		case "${ARCH}" in
			x86_64-musl)
			./install-tl -profile codelabs.profile -force-platform x86_64-linuxmusl
			;;
			*)
			./install-tl -profile codelabs.profile
			;;
		esac
	esac
esac
